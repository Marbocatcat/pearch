from setuptools import setup, find_packages

setup(
    name = 'pearch',
    version = '0.1',
    author = 'Mar Bocatcat',
    author_email = 'mar.bocatcat@gmail.com',
    description = 'my own integration of the popular cat UNIX command.',
    license = 'MIT',
    url = 'https://github.com/Marbocatcat/pearch.git',
    packages = find_packages(),
    entry_points = '''
        [console_scripts]
        pearch = pearch.pearch:main
    '''
)
